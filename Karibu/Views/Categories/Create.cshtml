@model Karibu.Models.Categorie

@{
    ViewData["Title"] = "Ajouter | Domaine du sujet";
    ViewData["PageTitle"] = ViewData["Title"];
}

<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Domaine" class="form-label"></label>
                <input asp-for="Domaine" class="form-control" />
                <span asp-validation-for="Domaine" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Enregistrer" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Retour à la liste</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
